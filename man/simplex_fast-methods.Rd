% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{simplex_fast-methods}
\alias{simplex_fast-methods}
\alias{coef.simplex_fast}
\alias{vcov.simplex_fast}
\alias{logLik.simplex_fast}
\alias{nobs.simplex_fast}
\alias{fitted.simplex_fast}
\alias{residuals.simplex_fast}
\alias{deviance.simplex_fast}
\alias{model.matrix.simplex_fast}
\alias{terms.simplex_fast}
\alias{formula.simplex_fast}
\alias{model.frame.simplex_fast}
\alias{update.simplex_fast}
\alias{confint.simplex_fast}
\title{Extractor Methods for Simplex Regression Fits}
\usage{
\method{coef}{simplex_fast}(object, model = c("all", "mean", "dispersion"), ...)

\method{vcov}{simplex_fast}(object, ...)

\method{logLik}{simplex_fast}(object, ...)

\method{nobs}{simplex_fast}(object, ...)

\method{fitted}{simplex_fast}(object, model = c("mean", "dispersion"), ...)

\method{residuals}{simplex_fast}(object, type = c("response", "pearson", "deviance"), ...)

\method{deviance}{simplex_fast}(object, ...)

\method{model.matrix}{simplex_fast}(object, model = c("mean", "dispersion"), ...)

\method{terms}{simplex_fast}(x, model = c("mean", "dispersion"), ...)

\method{formula}{simplex_fast}(x, ...)

\method{model.frame}{simplex_fast}(formula, ...)

\method{update}{simplex_fast}(object, formula., ..., evaluate = TRUE)

\method{confint}{simplex_fast}(object, parm, level = 0.95, ...)
}
\arguments{
\item{object, x, formula}{An object of class \code{"simplex_fast"} (the argument is
named \code{object}, \code{x} or \code{formula} to match the corresponding generic).}

\item{model}{For \code{coef}, one of \code{"all"}, \code{"mean"} or \code{"dispersion"}; for
\code{fitted}, \code{model.matrix} and \code{terms}, one of \code{"mean"} or \code{"dispersion"}.}

\item{...}{Additional arguments, currently ignored.}

\item{type}{For \code{residuals}, one of \code{"response"}, \code{"pearson"} or
\code{"deviance"}.}

\item{formula.}{For \code{update}, a change to the model formula, following the
conventions of \code{\link[stats:update.formula]{stats::update.formula()}}; the two-part mean/dispersion
structure is handled via the \pkg{Formula} package.}

\item{evaluate}{For \code{update}, logical; if \code{TRUE} (default) the updated call
is evaluated and the refitted model returned, otherwise the updated call is
returned unevaluated.}

\item{parm}{For \code{confint}, a specification of which parameters to report,
either a vector of numeric indices or of names. Defaults to all.}

\item{level}{For \code{confint}, the confidence level.}
}
\value{
\code{coef}, \code{fitted}, \code{residuals} and \code{nobs} return numeric vectors;
\code{vcov}, \code{confint} and \code{model.matrix} return matrices; \code{logLik} returns a
\code{"logLik"} object; \code{deviance} returns a single number; \code{terms}, \code{formula}
and \code{model.frame} return the corresponding model-description objects.
}
\description{
Standard extractor methods for objects of class \code{"simplex_fast"} produced by
\code{\link[=fastsimplexreg]{fastsimplexreg()}}. They mirror the conventions of the corresponding generics
for linear and generalised linear models.

\describe{
\item{\code{coef}}{Returns the estimated coefficients. \code{model = "all"} returns
the full vector, \code{"mean"} the mean submodel and \code{"dispersion"} the
dispersion submodel.}
\item{\code{vcov}}{Returns the variance-covariance matrix of the estimates.
Errors if the model was fitted with \code{inference = FALSE}.}
\item{\code{logLik}}{Returns the maximised log-likelihood, with attributes \code{df}
(number of estimated parameters) and \code{nobs}, and class \code{"logLik"}.}
\item{\code{nobs}}{Returns the number of observations used in the fit.}
\item{\code{fitted}}{Returns the fitted means (\code{model = "mean"}) or fitted
dispersions (\code{model = "dispersion"}).}
\item{\code{residuals}}{Returns residuals. \code{type = "response"} gives
\eqn{y - \hat\mu}; \code{type = "pearson"} gives
\eqn{(y - \hat\mu) / \sqrt{\hat\phi\, V(\hat\mu)}} with the simplex unit
variance function \eqn{V(\mu) = \{\mu(1-\mu)\}^3}, i.e. the first-order
dispersion-model approximation \eqn{\mathrm{Var}(Y) \approx \phi\,V(\mu)};
\code{type = "deviance"} gives the signed deviance residual
\eqn{\mathrm{sign}(y-\hat\mu)\sqrt{d(y;\hat\mu)/\hat\phi}} based on the
simplex unit deviance
\eqn{d(y;\mu) = (y-\mu)^2 / \{y(1-y)\mu^2(1-\mu)^2\}}.}
\item{\code{confint}}{Returns Wald confidence intervals
\eqn{\hat\theta \pm z_{1-\alpha/2}\,\mathrm{SE}(\hat\theta)}, using the
covariance matrix from \code{vcov}.}
\item{\code{deviance}}{Returns the scaled deviance
\eqn{\sum_i d(y_i;\hat\mu_i)/\hat\phi_i}, equal to the sum of squared
deviance residuals.}
\item{\code{model.matrix}}{Returns the mean (\code{model = "mean"}) or dispersion
(\code{model = "dispersion"}) design matrix. Requires the fit to have stored
the design (\code{x = TRUE}) or the model frame (\code{model = TRUE}).}
\item{\code{terms}}{Returns the \code{"terms"} object for the mean or dispersion
submodel.}
\item{\code{formula}}{Returns the (multi-part) model formula.}
\item{\code{model.frame}}{Returns the stored model frame (requires
\code{model = TRUE} at fitting time).}
\item{\code{update}}{Refits the model with a modified formula and/or arguments.
The multi-part (\code{|}) structure is updated correctly through the
\pkg{Formula} package.}
}
}
\examples{
set.seed(1)
n <- 300
dat <- data.frame(x1 = rnorm(n), z1 = rnorm(n))
mu <- simplex_linkinv(0.2 + 0.7 * dat$x1, link = "logit")
dat$y <- rsimplex(n, mu, exp(-0.5 + 0.4 * dat$z1))
fit <- fastsimplexreg(y ~ x1 | z1, data = dat, n_threads = 1L, x = TRUE)
coef(fit)
coef(fit, model = "mean")
vcov(fit)
logLik(fit)
AIC(fit)
nobs(fit)
deviance(fit)
head(fitted(fit))
head(residuals(fit, type = "deviance"))
confint(fit)
head(model.matrix(fit, model = "mean"))
formula(fit)

}
\seealso{
\code{\link[=fastsimplexreg]{fastsimplexreg()}}, \code{\link[=summary.simplex_fast]{summary.simplex_fast()}}, \code{\link[=predict.simplex_fast]{predict.simplex_fast()}},
\code{\link[=plot.simplex_fast]{plot.simplex_fast()}}, \code{\link[=simulate.simplex_fast]{simulate.simplex_fast()}}
}
