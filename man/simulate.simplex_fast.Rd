% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{simulate.simplex_fast}
\alias{simulate.simplex_fast}
\title{Simulate Responses from a Simplex Regression Fit}
\usage{
\method{simulate}{simplex_fast}(object, nsim = 1, seed = NULL, ...)
}
\arguments{
\item{object}{An object of class \code{"simplex_fast"}.}

\item{nsim}{Number of response vectors to simulate.}

\item{seed}{Optional seed for the random number generator. Handled following
the convention of \code{\link[stats:simulate]{stats::simulate()}}: when supplied, the current RNG state
is restored on exit and the seed is recorded in the \code{"seed"} attribute of
the result.}

\item{...}{Additional arguments, currently ignored.}
}
\value{
A \code{data.frame} with \code{nsim} columns (\code{sim_1}, \code{sim_2}, ...), each a
simulated response of length \code{nobs(object)}.
}
\description{
Simulates new response vectors from a fitted \code{"simplex_fast"} model by drawing
from the simplex distribution at the fitted means \eqn{\hat\mu_i} and
dispersions \eqn{\hat\phi_i}, using \code{\link[=rsimplex]{rsimplex()}}.
}
\examples{
set.seed(5)
dat <- data.frame(x1 = rnorm(200))
dat$y <- rsimplex(200, simplex_linkinv(0.3 + 0.5 * dat$x1, "logit"), 1)
fit <- fastsimplexreg(y ~ x1, data = dat, n_threads = 1L)
sims <- simulate(fit, nsim = 3, seed = 42)
str(sims)

}
\seealso{
\code{\link[=rsimplex]{rsimplex()}}, \code{\link[=fastsimplexreg]{fastsimplexreg()}}
}
