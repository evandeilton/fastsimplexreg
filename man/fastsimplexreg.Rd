% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplexreg.R
\name{fastsimplexreg}
\alias{fastsimplexreg}
\title{Fit a Fast Simplex Regression with Variable Dispersion}
\usage{
fastsimplexreg(
  formula,
  data,
  link = c("logit", "probit", "cloglog", "neglog"),
  start = NULL,
  maxit = 300L,
  rel_tol = 1e-09,
  grad_tol = 1e-06,
  n_threads = 1L,
  inference = TRUE,
  hessian_rel_step = 1e-05,
  trace = FALSE,
  subset = NULL,
  na.action = stats::na.omit,
  model = TRUE,
  x = FALSE,
  y = TRUE
)
}
\arguments{
\item{formula}{A multi-part formula, for example \code{y ~ x1 + x2 | z1 + z2}.}

\item{data}{A \code{data.frame} containing the response and covariates.}

\item{link}{Character string selecting the mean link: \code{"logit"}, \code{"probit"},
\code{"cloglog"} or \code{"neglog"}.}

\item{start}{Optional numeric starting vector \code{c(beta, gamma)}. When \code{NULL},
fast link-specific starting values are used.}

\item{maxit}{Integer; the maximum number of BFGS iterations.}

\item{rel_tol}{Numeric; relative tolerance on the objective function.}

\item{grad_tol}{Numeric; tolerance on the infinity norm of the gradient.}

\item{n_threads}{Integer number of OpenMP threads. Use \code{0} to request all
threads available to the backend.}

\item{inference}{Logical; if \code{TRUE}, computes the Hessian, the
variance-covariance matrix and the standard errors.}

\item{hessian_rel_step}{Numeric; the initial relative step for the Hessian,
obtained by central differences of the analytic gradient.}

\item{trace}{Logical; if \code{TRUE}, prints optimiser progress.}

\item{subset}{Optional vector specifying a subset of observations.}

\item{na.action}{A function indicating how to handle missing values.}

\item{model}{Logical; if \code{TRUE}, stores the model frame in the fitted object.}

\item{x}{Logical; if \code{TRUE}, stores the design matrices \code{X} and \code{Z}.}

\item{y}{Logical; if \code{TRUE}, stores the response in the fitted object.}
}
\value{
An object of S3 class \code{"simplex_fast"}: a list whose main components
are \code{coefficients} (a list with \code{mean} and \code{dispersion} estimates), \code{par}
(the full coefficient vector), \code{standard_errors}, \code{vcov}, \code{fitted.values}
(fitted means), \code{dispersion.values} (fitted dispersions),
\code{linear.predictors}, \code{residuals} (response residuals), \code{logLik}, \code{AIC},
\code{BIC}, \code{nobs}, \code{df.residual}, \code{convergence}, \code{message}, \code{iterations} and
the stored \code{terms}/\code{design} metadata used for prediction.
}
\description{
Fits, by maximum likelihood, a simplex regression model with separate
submodels for the mean and the dispersion. The interface uses the multi-part
formulas of the \pkg{Formula} package:

\code{y ~ x1 + x2 | z1 + z2}

The first right-hand side component models the mean \eqn{\mu}; the second
component models the dispersion \eqn{\phi}. When the second component is
omitted, as in \code{y ~ x1 + x2}, the dispersion is constant (equivalent to
\verb{| 1}).

The mean supports the \code{logit}, \code{probit}, \code{cloglog} and \code{neglog} links; the
dispersion uses a log link. The log-likelihood, the analytic score, the link
inverses and the BFGS optimiser run entirely in C++. Matrix-vector products
use Armadillo/BLAS and the per-observation loop may use OpenMP.
}
\examples{
# Simulated data with variable dispersion.
set.seed(123)
n <- 500
dat <- data.frame(x1 = rnorm(n), x2 = rbinom(n, 1, 0.4), z1 = rnorm(n))
mu <- simplex_linkinv(-0.4 + 0.8 * dat$x1 - 0.5 * dat$x2, link = "logit")
phi <- exp(-1 + 0.6 * dat$z1)
dat$y <- rsimplex(n, mu, phi)

fit <- fastsimplexreg(y ~ x1 + x2 | z1, data = dat, link = "logit",
                   n_threads = 1L)
summary(fit)
coef(fit)
head(predict(fit, type = "both"))

# Real data: reading accuracy from the 'betareg' package.
if (requireNamespace("betareg", quietly = TRUE)) {
  data("ReadingSkills", package = "betareg")
  rs <- fastsimplexreg(accuracy ~ dyslexia + iq | dyslexia,
                       data = ReadingSkills, link = "logit")
  summary(rs)
}

}
\references{
Barndorff-Nielsen, O. E. and Jorgensen, B. (1991).
Some parametric models on the simplex.
\emph{Journal of Multivariate Analysis}, \strong{39}(1), 106--116.

Zhang, P., Qiu, Z. and Shi, C. (2016).
simplexreg: An R Package for Regression Analysis of Proportional Data Using
the Simplex Distribution.
\emph{Journal of Statistical Software}, \strong{71}(11), 1--21.
}
\seealso{
\code{\link[=dsimplex]{dsimplex()}}, \code{\link[=rsimplex]{rsimplex()}}, \code{\link[=simplex_linkinv]{simplex_linkinv()}},
\code{\link[=predict.simplex_fast]{predict.simplex_fast()}}, \code{\link[=summary.simplex_fast]{summary.simplex_fast()}}
}
