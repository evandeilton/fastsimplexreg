% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{summary.simplex_fast}
\alias{summary.simplex_fast}
\alias{print.summary.simplex_fast}
\title{Summarise a Simplex Regression Fit}
\usage{
\method{summary}{simplex_fast}(object, ...)

\method{print}{summary.simplex_fast}(x, digits = max(3L, getOption("digits") - 3L), ...)
}
\arguments{
\item{object}{An object of class \code{"simplex_fast"}.}

\item{...}{Additional arguments, currently ignored.}

\item{x}{An object of class \code{"summary.simplex_fast"}.}

\item{digits}{Integer; the number of significant digits to display.}
}
\value{
An object of class \code{"summary.simplex_fast"}, a list whose main
component \code{coefficients} is itself a list with the \code{mean} and \code{dispersion}
coefficient tables (each with columns \code{Estimate}, \verb{Std. Error}, \verb{z value}
and \verb{Pr(>|z|)}), together with the Pearson residuals, the links, fit
statistics (log-likelihood, AIC, BIC, deviance) and optimiser diagnostics.
The \code{print} method returns its argument invisibly.
}
\description{
Produces a summary of a fitted \code{"simplex_fast"} object, including coefficient
tables with standard errors, Wald z-statistics and p-values for both the mean
and dispersion submodels.
}
\examples{
set.seed(4)
dat <- data.frame(x1 = rnorm(300), z1 = rnorm(300))
mu <- simplex_linkinv(0.2 + 0.6 * dat$x1, "logit")
dat$y <- rsimplex(300, mu, exp(-0.4 + 0.3 * dat$z1))
fit <- fastsimplexreg(y ~ x1 | z1, data = dat, n_threads = 1L)
summary(fit)

}
\seealso{
\code{\link[=fastsimplexreg]{fastsimplexreg()}}, \code{\link[=print.simplex_fast]{print.simplex_fast()}}
}
