% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{predict.simplex_fast}
\alias{predict.simplex_fast}
\title{Predictions from a Simplex Regression Fit}
\usage{
\method{predict}{simplex_fast}(
  object,
  newdata = NULL,
  type = c("response", "mean", "dispersion", "link", "both"),
  ...
)
}
\arguments{
\item{object}{An object of class \code{"simplex_fast"}.}

\item{newdata}{Optional \code{data.frame} of new observations. When \code{NULL}, the
fitted values are returned.}

\item{type}{Type of prediction: \code{"response"} or \code{"mean"} (fitted mean
\eqn{\mu}), \code{"dispersion"} (fitted \eqn{\phi}), \code{"link"} (a list with the
linear predictors \code{mean} and \code{dispersion}) or \code{"both"} (a \code{data.frame}
with columns \code{mu} and \code{phi}).}

\item{...}{Additional arguments, currently ignored.}
}
\value{
A numeric vector, a list or a \code{data.frame}, depending on \code{type}.
}
\description{
Computes predictions from a fitted \code{"simplex_fast"} model, either on the
data used for fitting or on new data. When \code{newdata} is supplied, the stored
\code{terms}, \code{xlevels} and \code{contrasts} are reused so that the design matrices are
built consistently with the fit.
}
\examples{
set.seed(2)
n <- 300
dat <- data.frame(x1 = rnorm(n), z1 = rnorm(n))
mu <- simplex_linkinv(0.1 + 0.6 * dat$x1, link = "logit")
dat$y <- rsimplex(n, mu, exp(-0.5 + 0.3 * dat$z1))
fit <- fastsimplexreg(y ~ x1 | z1, data = dat, n_threads = 1L)
head(predict(fit, type = "response"))
head(predict(fit, newdata = dat[1:5, ], type = "both"))

}
\seealso{
\code{\link[=fastsimplexreg]{fastsimplexreg()}}
}
