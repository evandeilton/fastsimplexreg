% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{dsimplex}
\alias{dsimplex}
\title{Simplex Distribution Density}
\usage{
dsimplex(x, mu, phi, log = FALSE, n_threads = 1L)
}
\arguments{
\item{x}{Numeric vector of observations. Values must lie strictly inside
\eqn{(0, 1)} to receive positive density.}

\item{mu}{Numeric vector of means in \eqn{(0, 1)}, of length one or
\code{length(x)}.}

\item{phi}{Numeric vector of positive dispersion values, of length one or
\code{length(x)}.}

\item{log}{Logical; if \code{TRUE}, log-densities are returned.}

\item{n_threads}{Integer number of OpenMP threads. Use \code{0} to request all
threads available to the backend. Defaults to \code{1L} (serial).}
}
\value{
A numeric vector of densities (or log-densities when \code{log = TRUE})
with the recycled length of \code{x}, \code{mu} and \code{phi}.
}
\description{
Evaluates the probability density function of the simplex distribution of
Barndorff-Nielsen and Jorgensen (1991) for a mean \code{mu} and a dispersion
\code{phi} (the parameter often written \eqn{\sigma^2}). The density is
\deqn{f(x; \mu, \phi) = [2\pi\phi\,(x(1-x))^3]^{-1/2}
  \exp\!\left\{-\frac{1}{2\phi}\,
  \frac{(x-\mu)^2}{x(1-x)\,\mu^2(1-\mu)^2}\right\},
  \qquad 0 < x < 1.}
Values of \code{x} outside the open interval \eqn{(0, 1)} return \code{0} (or \code{-Inf}
on the log scale). The arguments \code{mu} and \code{phi} are recycled against \code{x}.
The computation is carried out in C++ and may use OpenMP threads.
}
\examples{
dsimplex(c(0.2, 0.5, 0.8), mu = 0.5, phi = 1)
dsimplex(c(0.2, 0.5, 0.8), mu = 0.5, phi = 1, log = TRUE)

# Integrates to one over the support.
integrate(function(u) dsimplex(u, mu = 0.4, phi = 2), 0, 1)$value

}
\references{
Barndorff-Nielsen, O. E. and Jorgensen, B. (1991).
Some parametric models on the simplex.
\emph{Journal of Multivariate Analysis}, \strong{39}(1), 106--116.
}
\seealso{
\code{\link[=rsimplex]{rsimplex()}}, \code{\link[=fastsimplexreg]{fastsimplexreg()}}
}
