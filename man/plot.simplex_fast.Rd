% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.simplex_fast}
\alias{plot.simplex_fast}
\title{Diagnostic Plots for a Simplex Regression Fit}
\usage{
\method{plot}{simplex_fast}(
  x,
  which = 1:4,
  type = c("deviance", "pearson", "response"),
  smooth = TRUE,
  ...
)
}
\arguments{
\item{x}{An object of class \code{"simplex_fast"}.}

\item{which}{Integer vector selecting the panels to draw, a subset of
\code{1:4}.}

\item{type}{Type of residual used in panels 1-3: one of \code{"deviance"},
\code{"pearson"} or \code{"response"}. See \code{\link[=residuals.simplex_fast]{residuals.simplex_fast()}}.}

\item{smooth}{Logical; if \code{TRUE} (the default) a LOESS smoother is added to
the residual and scale-location panels when there are enough observations.}

\item{...}{Additional arguments, currently ignored.}
}
\value{
Invisibly, a single \code{ggplot} object when one panel is requested, a
combined \pkg{patchwork} object when several panels are requested and
\pkg{patchwork} is available, or a named list of \code{ggplot} objects otherwise.
}
\description{
Produces model-diagnostic plots for a fitted \code{"simplex_fast"} object using
\pkg{ggplot2}. Up to four panels are available:

\enumerate{
\item Residuals against the fitted mean \eqn{\hat\mu}.
\item Normal quantile-quantile plot of the residuals, with a reference line
through the first and third quartiles.
\item Scale-location plot (\eqn{\sqrt{|\text{residual}|}} against
\eqn{\hat\mu}).
\item Observed response against the fitted mean, with the identity line.
}
}
\details{
Each panel is a self-contained \code{ggplot} object. When more than one panel is
requested the panels are combined into a single figure with \pkg{patchwork}
if that package is installed; otherwise they are drawn one after another. The
(list of) \code{ggplot} object(s) is returned invisibly, so the plots can be
further customised or re-arranged by the caller.
}
\examples{
set.seed(6)
n <- 400
dat <- data.frame(x1 = rnorm(n), z1 = rnorm(n))
mu <- simplex_linkinv(0.2 + 0.7 * dat$x1, link = "logit")
dat$y <- rsimplex(n, mu, exp(-0.5 + 0.4 * dat$z1))
fit <- fastsimplexreg(y ~ x1 | z1, data = dat, n_threads = 1L)
p <- plot(fit, which = 1:4)
# 'p' is a ggplot/patchwork object that can be further customised.

}
\seealso{
\code{\link[=fastsimplexreg]{fastsimplexreg()}}, \code{\link[=residuals.simplex_fast]{residuals.simplex_fast()}}
}
